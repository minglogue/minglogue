google search console 등록하기


도메인이 없으면 URL 접두어에 링크 넣고
HTML 태그방식으로 하기.
`<body>`섹션 전에 나오는 `<head>`섹션에 붙여넣기

![[google-search-console-html-verification.png|363]]

홈페이지에 사이트맵 만들기

나의 경우 app 폴더 안에 /sitemap.xml 생성
```tsx
import type { MetadataRoute } from "next";
import { getAllPosts } from "@/lib/posts";

const siteUrl =
  "https://blog.minglogue.workers.dev";

export default function sitemap(): MetadataRoute.Sitemap {
  const posts = getAllPosts();

  const postPages = posts.map((post) => ({
    url: `${siteUrl}/posts/${post.slug}`,
    lastModified: new Date(post.date),
  }));

  return [
    {
      url: siteUrl,
      lastModified: new Date(),
    },
    {
      url: `${siteUrl}/coding`,
      lastModified: new Date(),
    },
    {
      url: `${siteUrl}/daily`,
      lastModified: new Date(),
    },
    {
      url: `${siteUrl}/pudding`,
      lastModified: new Date(),
    },
    ...postPages,
  ];
}
```

만든 후 사이트맵 제출
![[google-search-console-sitemap-submission.png|431]]
